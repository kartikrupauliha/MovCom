<% layout('layouts/boilerplate') %> 
<link rel="stylesheet" href="/stylesheets/home.css">


<h1 class="text-white">Upcoming Movies</h1>

<!-- <% for (let movie of movies) { %>
    <div class="card mb-3">
        <div class="row">
            <div class="col-md-4">
                <img class="img-fluid" alt="" src="<%=movie.image%>">
            </div>
            <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title"><%= movie.title %> </h5>
                    <p class="card-text"><%= movie.overview %> </p>
                    <p class="card-text">
                        <small class="text-muted"><%= movie.release_date %> </small> 
                    </p>
                    <a class="btn btn-primary" href="/upcoming-movies/<%= movie._id %>">View <%= movie.title %> </a>
                </div>
            </div>
        </div>
    </div>

<% } %>    -->

<% for (let movie of movies) { %>
    <div class="container mt-5 mb-5">
        <div class="row">
            <div class="col-sm-4">
                <div class="card">
                    <img class="card-img-top img-fluid" src="<%=movie.image%>" alt="Card image cap">
                    <div class="card-block">
                        <h4 class="card-title"><%= movie.title %></h4>
                        <p class="card-text ellipsis"><%= movie.overview %></p>
                        <!-- <p class="card-text">
                            <small class="text-muted"><%= movie.release_date %> </small> 
                        </p> -->
                        <a class="btn btn-primary" href="/upcoming-movies/<%= movie._id %>">View <%= movie.title %> </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
<% } %> 